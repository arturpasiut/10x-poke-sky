---
import MainLayout from "@/layouts/MainLayout.astro";
import UiShowcase from "@/components/examples/ui-showcase";
import ThemeToggle from "@/components/ui/theme-toggle";
---

<MainLayout title="UI Preview">
  <Fragment slot="header">
    <div class="flex flex-col gap-6 md:flex-row md:items-start md:justify-between">
      <div class="space-y-3">
        <p class="text-sm uppercase tracking-[0.3em] text-muted-foreground">Design system</p>
        <h1 class="text-3xl font-semibold tracking-tight text-foreground sm:text-4xl">Podgląd komponentów UI</h1>
        <p class="max-w-2xl text-base text-muted-foreground">
          Ta strona prezentuje nowo utworzone komponenty shadcn/ui w kontekście layoutu aplikacji. Użyj zakładek i
          filtrów, aby zobaczyć style w akcji w trybie light i dark.
        </p>
      </div>
      <ThemeToggle client:load />
    </div>
  </Fragment>

  <UiShowcase client:load />
</MainLayout>
